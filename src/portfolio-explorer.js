import { startupFund, filterFundCompanies } from './portfolio-model.js';
import {otherFundAssets} from './fund-other-assets.js';
import {portfolioBreakdown,updatePortfolioExposure} from './portfolio-breakdown.js';
import { escapeHTML as esc } from './chain-utils.js';
const fund=startupFund;
const $=s=>document.querySelector(s);
const external=(url,label,cls='')=>`<a class="${cls}" href="${esc(url)}" target="_blank" rel="noopener noreferrer">${label} ↗</a>`;
const icon=c=>c.image?`<img data-company-mark="${esc(c.id)}" src="${esc(c.image)}" alt="" width="56" height="56" loading="lazy">`:`<span class="fund-monogram" aria-hidden="true">${esc(c.name.slice(0,2))}</span>`;
window.CapitalFund={...fund,icon};
const workflow=`<ol class="fund-claim-steps"><li><span>01</span><div><h3>Receive & buy</h3><p>Capital receives creator fees and completes a permitted VCXx purchase. The receipt records the network, amount and transaction.</p></div></li><li><span>02</span><div><h3>Set the distribution</h3><p>Designate how many purchased VCXx tokens to distribute and publish the eligibility and allocation rules.</p></div></li><li><span>03</span><div><h3>Record the snapshot</h3><p>Fix eligible holders and their weights at a recorded block. Later transfers cannot increase this distribution’s allocation.</p></div></li><li><span>04</span><div><h3>Fund the claims</h3><p>Fund a reviewed distribution contract with the allocated tokens before any claim opens.</p></div></li><li><span>05</span><div><h3>Claim on Capital</h3><p>Eligible holders click Claim VCXx. A confirmed token transfer updates their wallet balance and claim receipt.</p></div></li></ol>`;
const explanation=`<p>VCXx is a tracker certificate issued by Backed Assets (JE) Limited, linked to Fundrise Innovation Fund shares (VCX). Holding VCXx gives exposure through that product; it does not give you separate shares in the companies shown here.</p><p>The companies and approximate weights below come from the fund’s June 30, 2026 disclosure. Weights refer to fund net assets, not company ownership. The directory lists 29 named equity companies; debt and cash-management investments appear separately. Some company exposure is held through investment vehicles, which are not counted again as companies.</p>`;
let returnFocus;
const dialog=document.createElement('dialog');dialog.id='fund-dialog';dialog.className='fund-dialog';dialog.setAttribute('aria-labelledby','fund-dialog-title');document.body.append(dialog);
function showDialog(html){returnFocus=document.activeElement;dialog.innerHTML=`<button class="icon-button dialog-close" data-close aria-label="Close fund details">×</button>${html}`;dialog.showModal();}
dialog.addEventListener('close',()=>{if(returnFocus?.isConnected)returnFocus.focus();});
dialog.addEventListener('click',event=>{if(event.target===dialog){const r=dialog.getBoundingClientRect();if(event.clientX<r.left||event.clientX>r.right||event.clientY<r.top||event.clientY>r.bottom)dialog.close();}});
function companyDetails(id){const c=fund.companies.find(c=>c.id===id);if(!c)return;showDialog(`<div class="fund-detail-identity">${icon(c)}<div><span class="fund-label">${esc(c.category)}</span><h2 id="fund-dialog-title">${esc(c.name)}</h2></div></div><p class="fund-detail-description">${esc(c.description)}</p><p>${esc(c.detail)}</p><div class="fund-exposure-path"><span>VCXx</span><i>→</i><span>VCX fund</span><i>→</i><span>${esc(c.name)}</span></div><div class="fund-detail-weight"><strong>${esc(c.weight)}</strong><span>Approximate share of fund net assets<br>Reported June 30, 2026</span></div><p class="fund-note">An indirect fund exposure. Buying more VCXx increases exposure to the fund as a whole; it does not buy this company separately. No Capital purchase or personal allocation is implied by this profile.</p><div class="fund-link-row">${external(c.website,'Company website','button button-dark')}${external(fund.holdingsUrl,'Fund disclosure','button button-light')}</div>`);}
function fundDetails(){showDialog(`<span class="fund-label">THE INVESTMENT ROUTE</span><h2 id="fund-dialog-title">One token. A fund of companies.</h2>${explanation}<div class="fund-exposure-path"><span>Your wallet</span><i>→</i><span>VCXx certificate</span><i>→</i><span>VCX fund exposure</span></div><p>VCX can trade above or below its net asset value. Company weights are dated estimates of the fund’s composition, not live values of your individual positions. Returns, income and distributions are not guaranteed.</p><div class="fund-link-row">${external(fund.productUrl,'VCXx product terms')}${external(fund.fundUrl,'Fundrise VCX')}${external(fund.holdingsUrl,'Reported holdings')}</div><p class="fund-note">Product and venue eligibility apply. U.S. persons and U.S. distribution are excluded. Brazil’s absence from a restricted-country list does not by itself authorize Capital’s distribution arrangement. No promise of KYC-free access.</p>`);}
function renderDirectory(){
 const query=$('#fund-search').value,sector=$('#fund-sector').value,niche=$('#fund-niche').getAttribute('aria-pressed')==='true';
 const companies=filterFundCompanies({query,sector,niche});
 $('#fund-result-count').textContent=`${companies.length} ${companies.length===1?'company':'companies'} · June 30, 2026 disclosure`;
 $('#fund-companies').innerHTML=companies.map(c=>`<article class="fund-company-row"><button class="fund-company-main" data-exposure-company="${c.id}" aria-label="View ${esc(c.name)} fund exposure">${icon(c)}<span><strong>${esc(c.name)}</strong><small>${esc(c.category)}</small><p>${esc(c.description)}</p></span></button><div class="fund-company-weight"><strong>${esc(c.weight)}</strong><small>of fund net assets</small></div><div class="fund-company-actions">${external(c.website,'Website')}<button data-exposure-company="${c.id}" aria-label="Details for ${esc(c.name)}">Details ↗</button></div></article>`).join('')||'<div class="fund-empty"><h3>No companies match.</h3><p>Try a different sector or search.</p><button class="button button-light" id="fund-clear">Clear filters</button></div>';
 $('#fund-clear')?.addEventListener('click',()=>{$('#fund-search').value='';$('#fund-sector').value='All';$('#fund-niche').setAttribute('aria-pressed','false');renderDirectory();$('#fund-search').focus();});
}
function initialize(){
 $('#fund-directory').innerHTML=`<div class="fund-summary"><div><span class="fund-label">VCXx / FUND EXPOSURE</span><h2>Inside the innovation fund</h2><p>Company exposure through VCXx — reported June 30, 2026. Explore ${fund.companies.length} named equity companies, from dog longevity to AI infrastructure.</p></div><button class="button button-light" data-fund-details>Understand VCXx ↗</button></div><p class="fund-note">Indirect exposure through a tracker certificate linked to VCX. These are fund disclosures, not Capital’s purchased assets or your personal shares. ${external(fund.holdingsUrl,'Source: June 30, 2026 holdings')}</p><div class="fund-toolbar"><div class="fund-filter-pills"><button id="fund-niche" aria-pressed="false">Niche companies</button></div><label><span class="sr-only">Filter fund companies by sector</span><select id="fund-sector"><option value="All">All sectors</option>${[...new Set(fund.companies.map(c=>c.category))].sort().map(c=>`<option>${esc(c)}</option>`).join('')}</select></label><label class="fund-search"><span class="sr-only">Search fund companies</span><input id="fund-search" type="search" placeholder="Search companies or ideas"></label></div><p id="fund-result-count" class="fund-result-count" role="status" aria-live="polite"></p><div id="fund-companies" class="fund-companies"></div>${otherFundAssets()}<p class="fund-note">Research checked September 14, 2026. Reported fund holdings can change. Company references do not imply endorsement or partnership.</p>`;
 $('#fund-search').addEventListener('input',renderDirectory);$('#fund-sector').addEventListener('change',renderDirectory);$('#fund-niche').addEventListener('click',()=>{const b=$('#fund-niche');b.setAttribute('aria-pressed',String(b.getAttribute('aria-pressed')!=='true'));renderDirectory();});renderDirectory();
 $('#fund-portfolio').innerHTML=`<div class="fund-portfolio-heading"><div><span class="fund-label">YOUR VCXx</span><h2>Fund exposure, in your wallet.</h2><p>A wallet balance is yours. Tokens held by Capital stay in the treasury until a confirmed distribution.</p></div><button class="text-button" data-fund-details>What am I holding? ↗</button></div><div class="fund-balance-grid"><article><span>Capital treasury</span><strong>— <small>VCXx</small></strong><p>Purchase wallet not connected</p><a href="#treasury">View treasury ↗</a></article><article><span>Your claim allocation</span><strong>— <small>VCXx</small></strong><p>No funded distribution published</p><button class="fund-claim-button" disabled aria-describedby="claim-unavailable">Claim VCXx</button></article><article><span>In your wallet · Ethereum</span><strong id="vcxx-balance">— <small>VCXx</small></strong><p id="vcxx-balance-status">Connect to read your token balance</p><button class="text-button" data-chain-connect id="vcxx-connect">Connect wallet ↗</button><button class="text-button" id="vcxx-refresh" hidden>Refresh balance ↻</button><a id="vcxx-wallet-link" target="_blank" rel="noopener noreferrer" hidden>View on explorer ↗</a></article></div><p id="claim-unavailable" class="fund-note">Claims open only after eligibility, a fixed snapshot and a funded production contract are in place. No real VCXx claims are connected yet.</p>${portfolioBreakdown(icon)}<section class="fund-receipts"><div><h3>Your distribution receipts</h3><span>No confirmed Capital distributions</span></div><p>A receipt will show the VCXx amount, snapshot, receiving wallet and confirmed transaction. Tokens you bought elsewhere are not Capital distribution receipts.</p></section><div class="fund-portfolio-links"><a href="#explore">Explore the companies inside VCX ↗</a><a href="#docs/distributions">How distributions will work ↗</a></div>`;
 $('#fund-distribution-process').innerHTML=`<div class="fund-section-heading"><span class="fund-label">FROM FEES TO HOLDERS</span><h2>The VCXx distribution process</h2><p>Purchases and distributions are separate steps, each with a verifiable record.</p></div>${workflow}<p class="fund-note">This is the intended production flow. Capital’s purchase wallet, holder snapshot rules and reviewed distribution contract are not connected.</p>`;
 $('#fund-docs').innerHTML=`<span class="fund-label">VCXx / FUND EXPOSURE</span><h2>The fund and your tokens</h2>${explanation}<div class="fund-link-row">${external(fund.productUrl,'Product terms')}${external(fund.holdingsUrl,'Fund holdings')}${external(fund.fundUrl,'Underlying fund')}</div><h3>How VCXx reaches a holder</h3>${workflow}<p>Example: if 100 VCXx are allocated and your eligible snapshot weight is 1%, your allocation is 1 VCXx. It becomes a wallet holding only after the transfer confirms. Neither a community vote nor buying Capital’s token creates a VCXx balance.</p><p>Claiming VCXx distributes the certificate token, not separate shares in the startups. Buying more tokens buys more of the whole fund exposure; Capital votes cannot change the fund manager’s portfolio weights.</p><p>Production needs permitted acquisition and distribution, published eligibility rules, a reproducible snapshot, full funding and a reviewed contract that handles VCXx’s token mechanics. Access may require provider identity checks. No distribution or yield is guaranteed.</p>`;
 document.addEventListener('click',e=>{const c=e.target.closest('[data-exposure-company]');if(c)companyDetails(c.dataset.exposureCompany);if(e.target.closest('[data-fund-details]'))fundDetails();});
 $('#vcxx-refresh').addEventListener('click',()=>readWallet(window.CapitalWallet?.getAccount()));
 document.addEventListener('mainstreet:wallet',e=>readWallet(e.detail?.account));readWallet(window.CapitalWallet?.getAccount());
}
let balanceRequest=0;
async function readWallet(account){
 const generation=++balanceRequest;
 $('#vcxx-balance').innerHTML='— <small>VCXx</small>';$('#vcxx-wallet-link').hidden=true;$('#vcxx-connect').hidden=!!account;$('#vcxx-refresh').hidden=!account;
 if(!account){$('#vcxx-balance-status').textContent='Connect to read your token balance';updatePortfolioExposure('disconnected');return;}
 updatePortfolioExposure('loading');
 $('#vcxx-balance-status').textContent='Reading confirmed Ethereum balance…';$('#vcxx-refresh').disabled=true;
 try{const response=await fetch(`/api/fund/balance?wallet=${encodeURIComponent(account)}`);const data=await response.json();if(!response.ok)throw new Error(data.error||'Balance unavailable');if(generation!==balanceRequest)return;
  const value=Number(data.balance);$('#vcxx-balance').innerHTML=`${esc(value===0?'0':value<0.000001?'<0.000001':value.toLocaleString('en-US',{maximumFractionDigits:6}))} <small>VCXx</small>`;
  updatePortfolioExposure(value>0?'held':'empty',value<0.000001?'<0.000001':value.toLocaleString('en-US',{maximumFractionDigits:6}));
  $('#vcxx-balance-status').textContent=`Block ${Number(data.block).toLocaleString('en-US')} · ${account.slice(0,6)}…${account.slice(-4)}`;
  const link=$('#vcxx-wallet-link');link.href=`https://etherscan.io/token/${fund.address}?a=${encodeURIComponent(account)}`;link.hidden=false;
 }catch(error){if(generation!==balanceRequest)return;$('#vcxx-balance-status').textContent='Balance could not be verified. Please retry.';updatePortfolioExposure('unavailable');}
 finally{if(generation===balanceRequest)$('#vcxx-refresh').disabled=false;}
}
initialize();
