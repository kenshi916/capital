import {sqliteTable,text,integer,uniqueIndex,index} from 'drizzle-orm/sqlite-core';
export const admins=sqliteTable('capital_admins',{slot:text('slot').primaryKey(),userId:text('user_id').notNull(),createdAt:integer('created_at').notNull()});
export const rounds=sqliteTable('capital_rounds',{
 id:text('id').primaryKey(),parentId:text('parent_id'),revision:integer('revision').notNull(),version:integer('version').notNull(),status:text('status').notNull(),mutationId:text('mutation_id').notNull(),payload:text('payload').notNull(),opensAt:integer('opens_at').notNull(),closesAt:integer('closes_at').notNull(),proposalHash:text('proposal_hash'),snapshotNumber:integer('snapshot_number'),snapshotHash:text('snapshot_hash'),snapshotTime:integer('snapshot_time'),finalResults:text('final_results'),createdAt:integer('created_at').notNull(),updatedAt:integer('updated_at').notNull()
},t=>[index('idx_capital_rounds_status').on(t.status),uniqueIndex('idx_capital_round_parent').on(t.parentId)]);
export const audit=sqliteTable('capital_round_audit',{id:text('id').primaryKey(),roundId:text('round_id').notNull(),action:text('action').notNull(),actor:text('actor').notNull(),detail:text('detail').notNull(),createdAt:integer('created_at').notNull()},t=>[index('idx_capital_audit_round').on(t.roundId,t.createdAt)]);
export const challenges=sqliteTable('capital_vote_challenges',{id:text('id').primaryKey(),roundId:text('round_id').notNull(),wallet:text('wallet').notNull(),userId:text('user_id').notNull(),sequence:integer('sequence').notNull(),candidate:text('candidate').notNull(),weight:text('weight').notNull(),typedData:text('typed_data').notNull(),expiresAt:integer('expires_at').notNull(),createdAt:integer('created_at').notNull()},t=>[index('idx_capital_challenge_user_time').on(t.userId,t.createdAt)]);
export const votes=sqliteTable('capital_votes',{id:text('id').primaryKey(),roundId:text('round_id').notNull(),wallet:text('wallet').notNull(),sequence:integer('sequence').notNull(),candidate:text('candidate').notNull(),weight:text('weight').notNull(),signature:text('signature').notNull(),typedData:text('typed_data').notNull(),acceptedAt:integer('accepted_at').notNull()},t=>[uniqueIndex('idx_capital_vote_wallet_sequence').on(t.roundId,t.wallet,t.sequence),index('idx_capital_vote_round').on(t.roundId,t.acceptedAt)]);
